# 变更日志

本文件用于记录项目中所有值得关注的代码与功能变更。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
正式发版时可按 [Semantic Versioning](https://semver.org/lang/zh-CN/) 维护版本号。

## [未发布]

### 新增

- 新增 DataSync 保守运行档、市场晴雨表同步任务和服务器运行保护配置，默认只注册核心行情与市场统计链路。
- 新增 Strategy V2 后台运行并发限制配置，默认同一 Web 进程只执行 1 个策略任务运行，避免全市场任务叠加。

### 变更

- 收敛 DataSync 默认执行行为，禁止启动时全任务同步、限制任务并发和采集并发，并阻止首次运行自动全历史回补，降低服务器 CPU 与内存峰值。
- 优化 Mongo 聚合、DataSync 失败重试和板块分析查询路径，减少无界结果集加载，并下调服务器示例中的 Mongo/Redis/LLM 并发默认值。
- 收敛推理节点、监听大股票列表、市场晴雨表回补、热点新闻和事件聚类的默认并发与批次，降低 4c8g 服务器运行尖峰。
- 收敛知识库、复盘工具和个股联动分析中的无界 Mongo 读取，增加默认上限和字段投影，避免历史数据堆积后占用过多内存。
- 为诊断、监听、订阅、重点股票集合和智能工作台的运行时查询补充显式上限，降低多用户和全市场分析场景下的内存尖峰。
- 为市场页、板块主线分析、市场晴雨表和报告日期查询补充查询上限与字段投影，降低页面访问造成的 Web 内存波动。
- 为交割单复盘列表、持仓重建、统计分析和 K 线详情补充字段投影、查询上限与超大记录保护，避免导入数据持续增长后一次性加载过多记录。
- 为 DataSync 单日统计、股票详情、选股股池和新闻去重补充字段投影与查询上限，降低定时统计和页面复盘的单次内存占用。
- 为监听持仓策略、支撑压力策略、股票全集同步、新闻采集和股票关联构建补充查询上限，减少后台任务在异常数据量下的内存风险。
- 为 Strategy V2 K 线策略参数增加窗口上限，并分批执行事件聚类增强任务，避免异常参数和批量 LLM 调用造成后台尖峰。
- 为 Strategy V2 任务列表、运行结果、目标解析、盘感练习会话和旧版回测因子查询补充硬上限，避免用户侧误触发无界读取。
- 为 Mongo 聚合封装增加默认结果上限、批大小和执行超时，并为新闻统计聚合与事件指纹提取补充分批保护。
- 为 Listener 单轮轮询、DataSync 手动热点刷新和订阅刷新通知补充超时、防抖与合并保护，减少后台任务堆积。
- 为 AgentServer 部署配置补充 MongoDB、Redis、Milvus 相关容器资源边界，并将示例环境变量调整为服务器保守默认。
- 为 MongoDB 启动索引检查增加可关闭开关和单集合创建超时，避免多节点重启时索引检查长期阻塞或放大数据库压力。
- 为 Strategy V2 一句话选股候选缓存和新闻快速去重内存缓存补充容量裁剪，避免常驻进程缓存随运行时间持续增长。
- 为 Redis 任务队列增加最大积压保护，并将通用 Mongo `insert_many` 改为分批写入，避免任务堆积和大批量插入造成内存尖峰。
- 为每日复盘数据子采集增加单集合行数上限，并复用通用 Mongo 分批写入，避免异常接口返回造成大批量写入尖峰。
- 为多源新闻采集增加来源并发和每源条数上限，并跳过未启用来源，降低高频新闻任务的外部请求与去重查询尖峰。
- 收窄每日统计回补检查的 Mongo 查询窗口，并将市场统计缓存预热周期改为可配置，默认不主动预热一年周期重查询。
- 将新闻生命周期压缩和过期事件清理改为按 ID 分批处理，避免历史新闻积压后单次更新或删除造成数据库写入尖峰。
- 同步 Docker 部署示例中的 DataSync 保守配置，确保容器启动时也透传复盘采集、市场统计预热和多源新闻限流参数。
- 将完整 Docker 编排中的 DataSync 环境变量与默认编排对齐，避免 full 部署遗漏保守运行参数。
- 为独立 DataSync 节点接入全局任务并发预算、任务硬超时和可配置锁超时，避免单个采集任务卡死或多个重任务叠加压垮服务器。
- 拆分独立 DataSync 的核心、后台与重型任务资源类别，保守档默认只注册核心市场链路，并支持白名单启用非核心任务。
- 为热点新闻采集增加默认关闭、来源白名单、每轮来源数、每源条数、HTTP 超时和整轮总超时保护，降低高频外部请求压力。
- 扩展独立 DataSync 任务运行记录模型，新增运行日期、目标交易日、数据截止时间、资源类别和数据源字段，保留旧同步标记兼容。
- 完善独立 DataSync 核心完整性检查，结合真实集合覆盖和目标交易日执行记录区分 ready、missing、warning 与 waiting window。
- 完善独立 DataSync ready marker 写入契约，顶层写入 ready 数据集、待补数据集、告警、检查时间与稳定状态。
- 为 Web 系统页和市场页补充核心市场数据可用性接口，统一优先读取 `readiness_markers`，不再让页面依赖旧同步标记猜测数据是否可用。
- 收紧独立 DataSync 核心缺口恢复结果，最新交易日恢复会从首个缺失数据集顺序重跑、未到同步窗口时跳过，并返回已完成步骤与失败点。
- 完善独立 DataSync 最近窗口自动补缺结果与部署配置，默认启动后和每 20 分钟检查最近 3 个交易日，只恢复核心数据并记录恢复、跳过和失败明细。
- 为独立 DataSync 增加当天核心数据 SLA 时间窗，区分等待、同步中、过窗缺失和失败告警状态，避免盘中误报并让 17:00 后缺口明确进入失败态。
- 为独立 DataSync 增加统一 HTTP 客户端基座，先接入 Coze 工作流源，统一超时、重试退避、限流与服务端错误分类，并把结构化错误纳入数据源调用统计。
- 收敛独立 DataSync 数据源回退策略，按能力和调用形态定义明确 source chain，支持环境变量覆盖，并在所有源失败时抛出链路错误而不是伪装成空数据。
- 为独立 DataSync 核心采集器增加入库前轻量校验与清洗入口，覆盖日 K、每日指标、指数日 K、涨跌停和板块资金流的必填字段、日期标准化与数值转换。
- 为独立 DataSync 增加死信/脏数据记录集合，校验失败样本会受控写入 `datasync_dead_letters`，并在 ops summary 中展示近期死信与告警。
- 为独立 DataSync 增加统一幂等批量写入工具 `bulk_upsert_batched`，强制业务主键、统一批次大小、返回 inserted/modified/matched/failed，并把采集器写入结果带入任务详情。
- 为独立 DataSync 增加任务 checkpoint 与断点续拉契约，股票日 K 增量和历史扫描可在中断后从已完成交易日或股票代码后继续，并支持 CLI 查看近期 checkpoint。
- 为独立 DataSync 增加闲时补缺窗口，默认只允许重型任务和显式 backfill/history 触发在 `00:00-08:00` 运行，手动恢复可通过 `--force` 明确绕过。
- 为独立 DataSync 增加历史补缺队列 `datasync_backfill_jobs`，支持去重入队、小批量领取、完成/暂停/失败状态流转和 CLI 入队/查看/单轮消费。
- 为独立 DataSync 核心数据集补齐统一定向恢复契约，恢复前校验交易日并统一返回来源、告警和失败项，避免历史补缺队列在非交易日或源异常时静默空跑。
- 为独立 DataSync 历史补缺队列增加每日预算与连续失败熔断，限制每晚任务数和外部请求数，并在运维总览中展示最近预算状态。
- 为独立 DataSync 增加 `--health` 与 `--ready` 轻量探针命令，并将独立 Compose healthcheck 接入配置、Redis、Mongo 级健康检查。
- 完善独立 DataSync `ops-summary` 总探针，新增 `operational_status`、稳定 `probe_checks` 和补缺队列状态摘要，便于监控脚本与人工排障共用。
- 新增独立 DataSync 生产补数操作手册，覆盖 Docker 环境最近核心数据补缺、单任务定向补缺、夜间补缺队列、完成确认、失败排查和高风险命令禁用边界。
- 收口 DataSync Docker 部署默认值，MongoDB/Redis 宿主机端口默认只绑定本机，拆分宿主机映射端口与容器内连接端口，并为独立 DataSync Compose 补充保守资源边界。
- 新增 DataSync 迁移边界与灰度计划，对照旧 AgentServer DataSync 与独立 DataSync 的任务、集合和运维能力。
- 迁移 `market_weather` 股票晴雨表到独立 DataSync，保守档默认注册，支持单交易日定向恢复，并避免将 Coze 工作流结果纳入核心行情 ready 硬门槛。
- 为独立 DataSync 增加任务静态能力声明协议和 RPC 能力清单出口，核心任务可声明目标集合、依赖、调度、资源等级、恢复能力和质量规则。
- 为独立 DataSync 生成机器可读数据能力目录，支持通过 CLI 打印或写入 YAML/JSON，便于后续 Web 能力状态页复用。
- 收敛 DataSync 与 AgentServer 的 Mongo 索引初始化，启动默认只检查核心集合索引，健康/就绪探针不触发索引维护，并提供 DataSync 手动索引补齐命令。
- 为独立 DataSync 增加结构化任务日志与坏数据日志采样，任务生命周期可定位 job、交易日、来源和触发方式，异常 payload 不再完整输出到 Docker 日志。
- 为独立 DataSync 的幂等批量写入增加动态批次调整，Mongo 写入变慢时自动缩小批次，稳定后仅恢复到配置上限，并在任务写入详情中记录批次历史。
- 为独立 DataSync 增加分数据源限流令牌桶和 429 源级冷却，Tushare、AKShare、BaoStock、Coze 可分别限速，限频源会被临时跳过并继续回退。
- 为系统能力状态页增加 DataSync 数据能力面板，展示核心交易日、缺失数据集、最近失败任务、补缺队列摘要，并复用安全补缺入口。
- 为独立 DataSync 能力目录和任务执行记录增加 `recoverability` 可恢复性声明，强时效或不可完整后补任务失败时会进入 warning/critical 运维事件。
- 在系统能力状态页展示 DataSync 可恢复性摘要，缺失数据集和最近失败任务会标注可补、尽力补或不可还原。
- 为系统能力状态页增加 DataSync 补缺入队入口，缺失且可恢复的数据集可加入夜间补缺队列，实际执行仍由 DataSync worker 按窗口和预算消费。
- 为系统能力状态页增加 DataSync 补缺队列人工操作入口，支持暂停、恢复和重试安全状态转换，并写入运维审计事件。
- 为系统能力状态页增加 DataSync 近期运维事件展示，有限读取 `ops_events` 并展示事件级别、来源、消息和关键 details。
- 为 DataSync 增加 Docker Compose 安全默认值回归测试，覆盖根 compose、full compose 和独立 DataSync compose 的保守档、并发、补缺预算、热点新闻开关和资源限制。
- 将独立 DataSync 的 UTC 时间戳写法从 `datetime.utcnow()` 统一调整为 `datetime.now(UTC)`，消除 Python 3.13 弃用警告并保留原 UTC 语义。
- 将独立 DataSync 的 Pydantic 协议模型配置迁移为 `ConfigDict`，并补充测试确保额外字段兼容和时间默认值不会在导入时冻结。
- 补充 DataSync 统一提交范围审查文档，明确最终提交建议纳入范围、默认排除文件、运行生成物边界和提交前验证矩阵。

## [0.2.15] - 2026-05-25

### 新增

- 新增交割单复盘中的交易分析 tab，参考量化回测与常规交割单分析口径，展示收益质量、交易质量、持仓风险、个股贡献榜、亏损来源榜和自动复盘诊断。

### 变更

- 强化提交规范说明，明确提交信息冒号后必须使用中文，并补充禁止使用英文短句的示例。

## [0.2.14] - 2026-05-25

### 新增

- 新增策略中心 V2 与场景任务 V2 的后端闭环，支持策略列表、策略查看、参数覆盖、任务创建、任务列表、任务详情、运行详情和任务删除。
- 新增策略任务运行能力，支持一次性运行、手动运行、定时任务 worker、运行进度、运行日志、动作审计、失败状态修复与运行控制基础能力。
- 新增策略任务动作执行链路，支持生成临时清单、加入股池、股池流转、通知动作和自定义股票监听列表维护。
- 新增 V2 内置策略，包括一句话选股策略、双响炮策略、海龟交易策略以及由旧版市场监听迁移来的可复用监听策略。
- 新增盘感练习策略双盲模式，支持按选股策略筛选训练样本，并展示策略名称、入选日期与入选原因。

### 变更

- 调整菜单结构，按模块维度组织市场与数据、选股与股池、策略与任务、交易与复盘、智能工作区、系统和旧版待下线入口。
- 调整旧版市场监听迁移方案，新增监听任务中的自定义股票列表和单股监听参数配置，便于后续逐步下线旧模块。
- 调整盘感练习页面信息密度，将当前价、当前盈亏、总盈亏、仓位和进度收敛到 K 线标题工具栏，减少重复状态行占用。

### 修复

- 修复策略任务参数覆盖中浮点数无法输入的问题，满足指数指标预警等阈值类参数配置。
- 修复任务列表关键词展示不准确的问题，将任务列表中的关键词调整为策略名。
- 修复任务详情打开为空页面的问题，并优化任务页面表格布局、状态展示与详情入口。
- 修复任务运行中缺少实时进度的问题，补充全市场扫描数量、总数与百分比展示。
- 修复盘感练习买入后 K 线交易标记丢失的问题，统一按交易日期排序后再匹配标记点。

## [0.2.13] - 2026-05-23

### 新增

- 新增策略 V2 任务中心、任务详情、运行结果和策略运行入口的前端原型，形成可点击的策略任务工作流。
- 新增策略任务创建步骤条弹窗，支持基础信息、策略参数、场景信息和调度动作的分步配置。
- 新增策略中心详情弹窗、参数编辑、相关任务列表和从策略中心创建任务的入口。
- 新增场景任务规则体系后端骨架，覆盖选股、监听、回测、模拟等场景类型，并梳理目标范围、调度方式、调度动作及组合用途文档。

### 变更

- 重构策略中心页面信息密度，保留创建策略入口、策略矩阵列表、查看 / 编辑 / 删除 / 创建任务 / 查看运行任务等核心操作，并将策略详情改为弹窗加 tab 展示。
- 重构场景任务创建流程，改为标准步骤条弹窗，并按场景类型、目标范围、调度方式和调度动作做联动禁用与参数配置。
- 调整策略中心和任务中心的页面布局，降低装饰性信息占比，提高策略、任务、运行状态和后续动作的可读性。

## [0.2.12] - 2026-05-22

### 新增

- 新增独立 `DataSync` 引导服务目录，为后续数据同步服务独立运行、独立配置和独立容器化打基础。
- 新增策略 V2 需求、功能清单、数据模型、页面流和任务组合文档，明确策略、场景任务、动作、股池和交割单分析的边界。

### 变更

- 优化监听与缓存运行开销，减少高频监听和运行期缓存对服务资源的压力。
- 调整监听快照机制，增加分层快照和增量推送，为后续监听任务拆分与实时更新提供基础。
- 预计算并缓存市场统计数据，降低前端看板和统计页面的重复计算成本。

### 修复

- 修复北交所 `BJ` 股票代码转换问题，校正 92 只股票的 `ts_code` 处理。

## [0.2.11] - 2026-05-18

### 新增

- 新增系统页面补漏同步入口，便于从页面触发缺失数据补齐。

### 变更

- 优化服务端内存使用，降低运行过程中因 OOM 导致服务崩溃的风险。
- 调整股票统计页面的顶部信息密度与布局层级，去除重复标题与重复说明，仅保留一处页面级描述并统一各子页控件对齐方式。
- 调整股票统计页的真实数据优先策略，移除误导性的本地演示股票回退，后端异常时改为显式提示而不是继续展示伪造名称与代码组合。
- 调整涨停雁阵图、龙头周期榜与连涨连跌榜的数据口径与展示一致性，统一通过真实元数据校正股票名称，并让连涨连跌榜支持按周期切换。
- 保存本地股票关联关系与数据源修复改动，补齐后续数据源修复的衔接上下文。

### 修复

- 修复 LLM 内存缓存清理任务启动时机问题，避免初始化阶段过早启动后台清理逻辑。

## [0.2.10] - 2026-05-17

### 新增

- 新增股票统计工作台，采用左侧二级菜单加右侧内容区结构，覆盖打板组与趋势组的多类盘后统计视图。
- 新增股票统计聚合接口组，支持交易日历、涨停快照、龙头周期、市场情绪、阶段涨幅、次日胜率、连涨连跌与高低点策略等后端聚合数据。

### 变更

- 调整股票统计页面的数据加载方式，改为后端优先、前端本地样本兜底，便于先验证真实口径再继续压缩页面信息密度。
- 调整 GitHub Actions 部署流程，支持通过 `GITEE_MIRROR_GITHUB_ACTOR` 跳过 Gitee 镜像同步触发的重复执行。

## [0.2.9] - 2026-05-16

### 新增

- 新增钉钉通知机器人的可选关键词配置，支持按机器人单独补充安全校验关键字。
- 新增交割单复盘中的盈亏矩形热力图标签页，用矩形分块方式按股票展示盈亏体量并支持跳转深度复盘。
- 新增将持仓股工作台并入交割单复盘标签页的工作流，统一持仓查看、批量操作与持仓沉浸复盘入口。

### 变更

- 调整交割单深度复盘容错与导航逻辑，即使当前股票不是股票、缺少日线数据或图表接口异常，也仍可查看关联交割单并继续切换上一条、下一条。
- 调整按股票汇总进入深度复盘时的切换顺序，统一按汇总列表顺序进行导航，并补充失败场景下的补数入口与快捷键支持。
- 调整持仓沉浸复盘返回路径，统一回到交割单复盘页中的持仓股标签，移除旧的独立持仓股页面入口。

## [0.2.8] - 2026-05-14

### 新增

- 新增基于 GitHub Actions 的 `feature-monitor` 分支自动部署工作流，支持通过 SSH 登录服务器拉取代码并重启部署。
- 新增服务器侧部署脚本与 CI/CD 说明文档，补充 GitHub Secrets、服务器初始化与首次验证流程。
- 新增轻量部署文件 `docker-compose.lite.yml`，默认仅启动核心节点以适配低配云服务器。
- 新增默认管理员用户自动初始化能力，便于首次部署后直接登录系统。
- 新增构建版本展示与部署后自动刷新能力，方便识别前端是否已切到最新构建。
- 新增稳健的 A 股全量同步脚本，便于在大批量补数场景下执行更可控的全市场同步。

### 变更

- 调整默认部署栈，自动部署默认切到轻量版 Docker Compose 配置。
- 调整注册页布局与统一 K 线面板按钮样式，提升高频操作场景下的界面一致性。
- 调整 MongoDB WiredTiger 缓存上限为 `1.5G`，降低低配机器部署时的内存压力。

### 修复

- 修复 Docker 部署与登录处理不稳定的问题，提升服务器侧自动拉起成功率。
- 修复前端 `uuid` 兼容兜底问题，避免部分环境下因依赖差异导致运行异常。
- 修复 `Tushare` 调用频率控制不一致的问题，统一补足限流保护。
- 修复监听交易时段的时区判断问题，避免市场时间策略在非预期时段运行。
- 修复 K 线复盘标记点对齐偏移问题。

## [0.2.7] - 2026-05-12

### 新增

- 新增一句话选股深度复盘入口，可直接从本次选股结果进入统一 K 线复盘工作区。
- 新增持仓沉浸式复盘页，支持在交割单持仓快照上下文中连续切换股票并复用统一 K 线面板。
- 新增盘感练习历史战绩页，并补充沉浸模式下的历史会话回看能力。

### 变更

- 抽离并统一多页面 K 线复盘组件，覆盖一句话选股、股池沉浸复盘、交割单沉浸复盘、个股详情、盘感练习与持仓沉浸复盘。
- 调整统一 K 线面板交互，补充左右切换、缩放、周期切换、通用信息压缩展示与顶部通用操作区。
- 调整交割单复盘结构，将“按股汇总”收回到与逐笔复盘、统计总览同级的 tab，并支持从股票汇总直接进入沉浸式复盘。
- 调整交割单沉浸式复盘页的图上标记展示，不再在 K 线头部重复显示买卖信息，并改为在图上携带数量标签。
- 调整盘感练习沉浸模式，统一复用通用 K 线面板，并清理重复标题与多余图表头部。

### 修复

- 修复一句话选股深度复盘与持仓沉浸复盘页中 K 线区域在部分布局下不可见的问题，改为显式约束图表容器高度。
- 修复个股详情页 K 线容器在统一面板接入后可能被压缩的问题。

## [0.2.6] - 2026-05-11

### 新增

- 新增用户维度的企业微信 / 钉钉机器人配置能力，并支持在监听策略上选择具体通知机器人。
- 新增个股详情页与交割单沉浸式复盘页的统一复盘上下文增强，补充周线、月线、近 30 日涨幅与概念信息。

### 变更

- 调整个股 AI 分析任务创建链路，前端改为走专用分析接口，并补充失败提示。
- 调整个股详情页与交割单沉浸式复盘页的数据与交互结构，使其逐步向统一 K 线复盘工作区靠拢。

### 修复

- 修复个股详情页部分数据接线与交互问题，补齐周/月线切换、补数状态轮询与上下文展示。
- 记录个股 AI 分析当前仍待继续排查的问题，避免后续线程衔接丢失上下文。

## [0.2.5] - 2026-05-11

### 新增

- 新增全市场“指数指标预警”监听策略，支持围绕涨跌幅、涨跌家数、涨跌停家数与资金流向做集中预警。
- 新增自动任务总览页，用于纯展示项目中的定时任务、持续机制与事件驱动自动流程。

### 变更

- 调整高频监听策略的不限次数提醒语义，统一抽象为“跨阈值触发模型”，只在首次越线和回落后二次越线时提醒。
- 调整候选池规则，对来源于一句话选股的候选股启用“5 个交易日后自动移出”机制。
- 调整全局专注模式入口，改为更稳定的全局悬浮按钮。

### 修复

- 修复指数监听策略在编辑参数时可能因无关依赖加载失败而打不开弹窗的问题。

## [0.2.4] - 2026-05-10

### 新增

- 新增“均线低吸”监听能力，替代固定 `5 日线低吸`，支持策略默认参数和单股独立均线周期配置。
- 新增撑压线的水平线 / 斜线双模式配置，支持只填价格的水平撑压线与双点位斜线。
- 新增在股池沉浸复盘页内直接配置单股特殊监听参数的能力，无需再切回市场监听页。
- 新增沉浸式复盘中的入池标记、周/月线切换、`MA60 / MA260` 与 `MACD` 展示。

### 变更

- 调整股池沉浸复盘页上下文，补充近 30 日涨幅、概念信息与单股补数提示说明。
- 调整复盘页中的专注模式入口与 K 线细节展示，持续向统一复盘工作流靠拢。

## [0.2.3] - 2026-05-10

### 新增

- 新增市场晴雨表历史回补、覆盖范围查询与基准回测仪表盘接口。
- 新增市场晴雨表页面的周期切换、图表 / 表格分 tab 展示和回测对照弹窗。

### 变更

- 调整市场晴雨表页面结构，优先展示当下建议、推荐仓位、超额收益与准确率，再把覆盖范围与明细表后置。
- 调整市场晴雨表图表，只保留推荐仓位和两条基准 / 策略净值曲线，减少无关干扰。
- 调整历史同步脚本容错，避免 Coze 单日异常阻塞整轮补数。

## [0.2.2] - 2026-05-10

### 新增

- 新增智能工作台草稿版，提供极简对话入口、流式消息与右侧 HTML 产物展示。
- 新增智能工作台只读分析骨架，可调用受控分析路径并生成 HTML 结果。

### 变更

- 调整智能工作台页面布局，逐步向官网式极简对话页收敛，并支持流式输出与结果侧栏。

## [0.2.1] - 2026-05-09

### 变更

- 调整单股异步补数任务的起点判定逻辑：当 `stock_basic.list_date` 缺失时，会优先尝试通过 `Tushare` 补齐上市日期，仍缺失时改用默认历史起点执行完整补数，不再退回只刷新本地已有区间。
- 调整单股补数任务结果结构，补充补数模式、起点来源与历史覆盖是否真正向前扩展等诊断信息，便于识别“任务成功但未补到更早历史”的场景。
- 调整手动全市场补数脚本的数据源路由：`stock_basic` 与 `daily_basic` 优先走 `Tushare`，单股历史 `stock_daily` 补数脚本支持显式指定优先数据源。
- 调整手动历史补库脚本参数，`daily_basic` 新增强制全量回补开关，`stock_daily` 支持跳过 `index_daily` 与 `limit_list`，便于专注股票主数据回补。
- 调整 `Tushare` 适配器与手动补数脚本的限频保护：Tushare 调用接入令牌桶节流，`daily_basic` / `stock_daily` 补数脚本在空数据、限频或累计失败时会中止并返回非零退出码，且不再把部分失败任务写成成功同步标记。

## [0.2.0] - 2026-05-09

### 新增

- 新增单股异步补数任务接口，支持为单只缺失数据股票创建后台补数任务，并查询任务进度、当前步骤与补齐结果。
- 新增股池沉浸式复盘页，支持在股池上下文中沿 K 线连续切换股票、快速加入自选、加监听以及复制/移动股票。
- 新增分钟异动监听策略，支持按最近 N 分钟涨跌幅超过阈值时触发短线预警。
- 新增持仓股页面，支持按交割单分组查看当前股票持仓、持仓成本、最新估值、浮盈亏和批量操作。
- 新增持仓盈亏阈值与盘中持仓盈亏变化两种监听策略，可直接基于交割单持仓分组做盘中预警。
- 新增固定止损与移动止损两种监听策略，支持按单只股票保存止损基准并在节点重启后继续生效。
- 新增 Coze 全量插件状态探测接口，可单独重复刷新 20 个扣子股票插件的可用状态、耗时、返回结构和项目接入情况。
- 新增交割单复盘第一版，支持分组管理、CSV 去重导入、逐笔复盘备注、基础统计和交易 K 线联动。
- 新增交割单沉浸式复盘页，支持 K 线、同股交易列表、复盘表单同屏协作，以及上一条、下一条的连续复盘流程。

### 变更

- 调整股池沉浸复盘页布局，进一步收紧头部与右侧操作区密度，并新增全局专注模式、单股补数入口与任务状态展示。
- 调整股池工作台，左侧按股池类型分组显示，并补充最新估值日、当日平均涨跌幅、来源股池和来源条件等摘要信息。
- 调整股池工作台表格交互，新增前端分页、来源字段中文展示、可换行提示和更紧凑的操作布局。
- 调整自动流转写入内容，目标股池中的股票会保留来源股池名称，便于后续复盘和追踪。
- 调整后端开发启动方式，支持 `web` 节点热更新以及开发栈统一通过 `--reload` 启动热更新模式。
- 调整策略监听页面，新增紧凑卡片、详细卡片与表格三种视图切换，并补充策略总数、启用数、监听数和流转数的概览统计。
- 调整策略监听页面统计口径，前端改为展示实际监听数量，并区分手动绑定、持仓分组和来源股池带来的监听覆盖范围。
- 调整侧边菜单分组与顺序，拆分为“新增与改造”和“原项目页面”两组，并按实际使用链路重排高频入口。
- 调整策略监听页面，将“监听频率”和“提醒频率”提取到独立的监听基础配置区块，为后续策略中心解耦做前置整理。
- 调整 `5日线低吸`、`涨跌停打开` 等策略的参数定义，补齐提醒频率、监控方向等基础参数并统一中文展示。
- 调整交割单导入后的处理流程，新增持仓快照自动重算与股票持仓默认过滤视图。
- 调整监听节点的监控股票收集逻辑，支持从交割单持仓快照自动扩展持仓分组的监控标的。
- 调整策略参数配置界面，支持为持仓监听策略直接选择交割单分组与波动方向。
- 调整策略监听配置界面，固定止损、移动止损与撑压线统一支持按股票单独配置和批量加入后的提示说明。
- 调整实时行情默认回退顺序，在 Coze 返回空时优先切到 Tushare，避免少量股票实时查询被 AKShare 全市场接口拖慢。
- 调整统一数据源优先级，使 Coze 优先承担单股实时、单股日线与财务类请求，免费源作为主要回退，Tushare 退居最后兜底。
- 调整系统状态页数据源区块，新增 Coze 插件状态明细与单独刷新入口，便于反复观察偶发失败和持续失败。
- 调整通用 K 线组件，支持按交易记录打买卖点标记，并统一按交易日升序绘制数据。
- 调整交割单复盘数据结构，将成败原因统一为“单一结果判定 + 原因标签”模型，并改为由后端按分组全量聚合统计。
- 调整交割单复盘列表页，补充分页、统计说明与进入沉浸式复盘的入口。
- 调整交割单 K 线复盘体验，支持同股切换时重新聚焦当前交易位置、当前记录高亮与未保存离开提醒。

### 修复

- 修复自动流转规则中的来源股池只参与流转、不参与实际监听范围的问题，来源股池内股票现在会自动并入 listener 的有效监控集合。
- 修复固定止损等策略在达到触发条件后仍按分钟重复提醒的问题，统一补充每日一次、提醒后关闭和不限次数三种提醒频率行为。
- 修复提醒状态只写入数据库但未同步回 listener 进程内存配置的问题，避免同一运行周期内重复触发。
- 修复交割单导入去重过粗导致“同日同价但资金余额不同”的独立成交被误判为重复，从而影响持仓快照的问题。
- 修复 Tushare 实时行情返回短代码 key 导致监听层按完整 `ts_code` 查不到数据的问题，并补充涨跌幅兜底计算。
- 修复 `stock_daily`、`daily_basic` 与 `index_daily` 自动同步在未真正落库时仍错误写入“已同步”标记的问题，避免节点整天跳过应补数据。
- 修复交割单复盘页只展示第一页数据却缺少翻页入口的问题。
- 修复交割单 K 线买卖点标记不可见或定位不明显的问题，改为轻量圆点加文字标注并落在真实成交价格轴线上。

## [0.1.1] - 2026-05-06

### 新增

- 新增盘感练习模块，支持从本地历史日线中随机抽取双盲样本进行逐根 K 线买卖练习。
- 新增 K 线练习后端会话接口，支持开局、续局、推进、交易、揭晓和自动平仓。
- 新增盘感练习工作台页面，支持按当前收盘价进行模拟买卖、查看持仓和复盘交易记录。
- 新增盘感练习沉浸模式与经典模式双页面，可按个人偏好切换训练视图。

### 变更

- 调整核心工作台菜单，前置盘感练习入口，便于与选股、股池、自选股形成连贯工作流。
- 调整盘感练习交互，补充键盘快捷键、详情抽屉与仓位快捷切换，并将沉浸模式默认 K 线范围改为全量视图。

### 修复

## [0.1.0] - 2026-05-05

### 新增

- 新增股池管理页面，可查看本地股池概览、池内股票和来源条件。
- 新增股池管理页手动新建股池入口，不再依赖一句话选股页面创建股池。
- 新增自选股与股池的批量加入策略监听能力，可将多只股票一次性加入同一监听策略。
- 新增监听触发事件记录、股池流转日志与第一版自动流转规则配置能力。
- 新增自选股批量移除能力，便于清理观察列表中的多只股票。
- 新增股池删除与池内股票批量移除能力，便于维护股池内容。
- 新增一句话选股模块，支持调用独立 Coze 工作流按自然语言筛股并记录查询结果。
- 新增最小可用股池能力，支持从一句话选股结果中勾选股票并加入本地股池。
- 新增市场晴雨表模块，支持通过独立 Coze 工作流获取市场因子、生成交易建议并按交易日落库。
- 新增市场晴雨表同步脚本与独立前端页面，可展示近一个月的因子变化和历史结论。
- 新增后端开发栈一键启动能力，可通过单条命令同时拉起 `web`、`data_sync`、`inference`、`listener`。
- 新增开发栈三级启动档位：`lite`、`standard`、`full`，方便按场景选择节点组合。
- 新增 Coze 工作流数据源适配器，并接入统一数据源管理器。
- 新增 Coze 单股基础信息、日线、实时行情、财务指标、每日指标与交易日历映射能力。
- 新增基于 Coze 的重点股票同步、全市场补库、能力检查和适配器验证脚本。
- 新增重点股票集合提取逻辑，以及按股票池补齐本地 `stock_daily` 和 `daily_basic` 的同步入口。
- 新增系统状态后端与前端页面，用于展示基础服务、数据源、数据集与功能接口状态。
- 新增数据库完整性、覆盖率和数据源路由可见性诊断能力。
- 新增监听通知 dry-run 能力及相关验证/测试覆盖。
- 新增监听告警预览能力，便于在不发送真实消息时验证通知内容。
- 新增撑压线监听策略，支持按单只股票配置支撑线与压力线的两组点位。
- 新增系统状态接口矩阵展示，可按能力场景对比多数据源状态与默认路由。
- 新增提交规范文档，并约定后续提交前需要更新本变更日志。

### 变更

- 调整侧边菜单分组与排序，将市场晴雨表、一句话选股和股池管理前置到核心工作台。
- 调整核心工作台分组，将自选股与市场监听前置到高频工作入口。
- 调整股池类型配置，股池管理页与一句话选股统一复用固定的股池类型选项。
- 调整一句话选股与市场晴雨表页面留白和表格滚动样式，提升宽表场景的可用性。
- 调整 Coze 工作流配置，补充一句话选股专用工作流 ID。
- 调整 Coze 工作流配置，补充市场晴雨表专用工作流 ID。
- 调整提交规范，后续提交信息统一采用 `type(scope): 中文说明` 格式。
- 调整本地开发启动方式，支持通过统一入口直接启动整套常用后端节点。
- 调整一键启动默认档位为 `full`，便于直接拉起最完整的页面功能依赖。
- 重构 `stock_daily` 与 `daily_basic` 同步流程，支持更安全的增量同步、重点股票同步与多源回退。
- 调整全市场日线补库策略，优先使用按交易日抓取与可用数据源回退，降低限频影响。
- 调整统一数据源默认调度规则，使单股、实时、财务类请求优先走更合适的可用数据源。
- 调整数据源配置项与调度参数过滤逻辑，避免多源调用时因额外参数导致适配器失败。
- 调整监听策略配置界面，支持在策略页按股票维护撑压线点位，并自动补全默认高低点价格。
- 重构系统状态页，改为概览加分区加载、缓存和接口矩阵展示。
- 调整系统状态后端接口，拆分为概览与分区请求，降低首屏阻塞时间。
- 调整订阅、监听与股票详情相关链路，使其与当前监听架构保持一致。
- 调整订阅默认参数与初始化脚本，使其与当前监听策略字段定义保持一致。
- 调整 Mongo 连接初始化超时配置，减少本地或受限环境下的长时间阻塞。

### 修复

- 修复股池工作台缺少删除入口的问题，补齐整池删除与池内股票删除链路。
- 修复自选股列表只能逐只移除的问题，补齐勾选后的批量删除操作。
- 修复市场晴雨表接口直接返回 Mongo 原始文档时导致的 `500` 序列化错误。
- 修复市场晴雨表历史同步串行执行导致的超时问题，改为并发拉取并保留跳过逻辑。
- 修复一句话选股结果结构与当前项目不一致的问题，统一补充 `code / ts_code / market` 元信息。
- 修复分析模块包级导入会阻塞新脚本与新接口的问题，改为惰性导出。
- 修复因本地 `stock_basic` 缺失导致的股票搜索异常。
- 修复多处 Tushare 集成问题，包括错误地址、健康检查与权限不足时的回退逻辑。
- 修复 AKShare 与 BaoStock 在最近交易日推断上的兼容问题。
- 修复统一数据源在单股与交易日相关请求上的默认路由与回退行为。
- 修复重点股票同步与全市场补库脚本的断点续跑、数据覆盖和完整性检查问题。
- 修复监听模块在策略参数、快照处理与通知链路上的兼容问题。
- 修复撑压线策略百分比阈值被误判为倍数的问题，避免远离线位时出现误报。
- 修复股票详情页在订阅记录缺失时无法正常补齐监听入口的问题。
- 修复前端构建阻塞项以及系统状态页若干展示问题。
- 修复报告页面类型检查与无用变量问题，避免能力状态页构建时被无关错误阻塞。
