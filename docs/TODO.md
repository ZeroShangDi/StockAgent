# 待办记录

本文件用于记录当前暂未处理、但后续需要继续推进的事项。

## 1. 补齐 `stock_daily` 全历史数据

- 状态：待处理
- 记录时间：2026-05-05
- 优先级：高

### 背景

当前数据库中的 `stock_daily` 已覆盖全部股票代码，但还不是“单只股票从上市至今的全量历史数据”。

当前检查结果：

- `stock_basic`：`5823`
- `stock_daily`：`8194027`
- `daily_basic`：`131101`
- `index_daily`：`12`

在有上市日期的 `5512` 只股票中：

- `3255` 只股票的最早日线日期晚于上市日期
- `3151` 只股票缺口超过 `1` 年
- `1016` 只股票缺口超过 `10` 年

### 当前判断

- 现有 `stock_daily` 可以满足一部分近期页面展示
- 但还不能视为“从上市至今全量”
- 对老股票、长周期回看、长期统计类功能仍然存在历史缺口

### 建议处理方式

优先只补 `stock_daily`，先不扩大到 `daily_basic` 和 `index_daily`。

建议使用现有脚本：

- [sync_market_from_coze.py](/Users/shangjunhao/Project/StockAgent/AgentServer/scripts/sync_market_from_coze.py)

注意事项：

- 需要使用 `--no-resume`
- 原因是默认断点续跑只检查结束日期是否存在，不适合补历史缺口
- 建议按股票池分批跑，不要一次性全市场全量重扫

### 建议命令

```bash
cd /Users/shangjunhao/Project/StockAgent/AgentServer

./venv/bin/python scripts/sync_market_from_coze.py \
  --start 19900101 \
  --end 20260430 \
  --skip-daily-basic \
  --skip-index-daily \
  --concurrent 4 \
  --no-resume \
  --offset 0 \
  --limit 300
```

后续按批继续推进：

```bash
--offset 300 --limit 300
--offset 600 --limit 300
...
```

### 时间预估

如果以当前 `Coze` 单股分段补历史的方式推进：

- 乐观：`8-20` 小时
- 常见：`1-3` 天

### 完成标准

- `stock_daily` 尽量覆盖到单只股票上市日附近
- 明显缺失多年历史的股票数量大幅下降
- 再次核查时，“最早日线晚于上市日”的股票数量显著减少

## 2. 完整股池流转机制

- 状态：待处理
- 记录时间：2026-05-05
- 优先级：高

### 背景

当前已落地的是“最小可用股池”：

- 一句话选股结果可勾选后加入本地股池
- 股池具备 `pool_type` 字段
- 股池内股票保留了来源模块、来源查询和来源 run_id

但这还只是后续复杂流转的起点，尚未覆盖完整的状态迁移与淘汰逻辑。

### 后续需要继续设计和开发的能力

- 明确股池类型体系，例如：
  - 候选池
  - 观察池
  - 监控池
  - 自选池
  - 交易预备池
- 设计股票在多个股池间的流转规则与状态机
- 定义从一个股池流入下一个股池的触发条件
- 定义淘汰条件、淘汰原因和淘汰时间
- 支持按来源模块追踪股票从“一句话选股”到后续池子的完整路径
- 为股池股票补充观察期、优先级、标签、备注和处理人等字段
- 视情况增加专门的股池管理页面，而不只是在选股页里做添加入口

### 建议方向

- 先保持当前 `stock_pools` 结构稳定运行
- 下一步优先补“池内股票状态”和“流转记录”两张表/两个集合
- 再决定是否需要独立的股池工作台页面
